import styled from 'styled-components'

export const ContainerStyle = styled.div`
    width: '100%',
    height: '800px',
`;

export const JitsiContainerStyle =  styled.div`
    display: (loading ? 'none' : 'block'),
    width: '100%',
    height: '100%',
`;